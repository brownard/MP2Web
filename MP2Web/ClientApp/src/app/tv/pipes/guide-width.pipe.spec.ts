import { GuideWidthPipe } from './guide-width.pipe';

describe('GuideWidthPipe', () => {
  it('create an instance', () => {
    const pipe = new GuideWidthPipe();
    expect(pipe).toBeTruthy();
  });
});
